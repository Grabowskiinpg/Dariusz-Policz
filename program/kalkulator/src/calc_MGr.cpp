//
// Created by User on 11.05.2019.
//

#include "calc_MGr.hpp"

/*
template<typename T>
Polynomial<T>::Polynomial(const std::vector<Monomial<T>>& vector){
    int max_ind=0;
    for(Monomial<T> monomial : vector) {
        if(monomial.get_index()>max_ind){
            max_ind=monomial.get_index();
        }
    }
    for(int i=0;i<=max_ind;i++){
        polynomial_[i]=Monomial(0,i);
    }
    for(Monomial<T> monomial : vector) {
        polynomial_[monomial.get_index()].
    }
}*/
